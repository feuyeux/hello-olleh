你现在是一名资深技术专家。请阅读当前仓库根目录下的 README、配置文件（如 package.json, pyproject.toml, go.mod 或 pom.xml）以及目录结构。

请输出一份项目初始化分析报告：
核心价值：这个项目解决了什么核心问题？
技术选型：识别主编程语言、核心依赖库、构建工具及其在项目中的角色。
目录地图：列出 Top 5 核心目录并说明其承载的业务逻辑或功能模块。
启动链路：指出程序的唯一入口点（Entry Point）在哪里，以及初始化的基本顺序。
分层模型：代码是如何分层的？（例如：CLI 交互层 -> 逻辑控制层 -> API 通讯层 -> 文件系统操作层）。
核心抽象：项目定义了哪些核心接口（Interfaces）或类（Classes）？它们是如何解耦的？
设计模式：在代码中发现了哪些经典设计模式？请举例说明其实现位置和解决的问题。
并发与状态管理：它是如何处理异步任务、长连接或全局状态的？

请以自然语言请求的生命周期为线索，详细分析其执行流程。请重点解析以下路径涉及的代码文件和函数：
用户输入解析：CLI 是如何捕获并预处理用户指令的？
Agent 决策链：代码中哪一部分负责调用 LLM？它是如何构建 Prompt 的？
工具调用（Tool Use）机制：它是如何动态调用 ls, grep, edit 等本地工具的？请分析其权限控制和执行闭环。
结果回传与 UI 更新：执行结果是如何反馈到 TUI 界面并更新进度的？

针对该项目的工程健壮性进行专项分析：
错误处理机制：项目如何捕获并恢复运行时异常？是否有重试策略或降级方案？
安全性分析：它是如何防止敏感文件泄露的？对于本地代码修改是否有安全限制（如只读区域）？
性能优化：在处理大型文件或大规模代码库时，采用了哪些性能优化手段（如索引、流式传输、增量扫描等）？
扩展性：如果我想为该工具增加一个新的 MCP（Model Context Protocol）工具，我需要修改哪些核心文件？

格式要求：
使用 Markdown 格式，包含清晰的目录导航；每个主题独立成篇，围绕实际代码进行阐述和展开。
关键流程必须附带 Mermaid 流程图/顺序图 代码，neutral主题；尽量左右布局。
每个核心模块必须列出 关键函数清单 及其功能描述。
包含一个 ‘代码质量评估’ 章节，指出当前实现的优缺点及潜在改进点。