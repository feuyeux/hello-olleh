遵循 `hello.txt` 中的要求，对 `opencode/` 目录下的工程进行深度分析，并将生成物写入 `hello-opencode/` 目录。

【强制输出结构】必须按以下文件清单逐一创建独立文件，每个文件只写对应主题，不得合并：

- `hello-opencode/00-opencode_ko.md` — Kickoff 索引：列出所有子篇超链接 + 最短源码阅读路线
- `hello-opencode/01-architecture.md` — 架构全景：目录结构、分层模型、核心抽象（Session、Provider、Tool）
- `hello-opencode/02-startup-flow.md` — 启动链路：入口点、CLI/TUI/Web 多表面初始化、Server 启动顺序
- `hello-opencode/03-agent-loop.md` — 核心执行循环：Session loop、Prompt 编译、LLM 调用、流式响应处理
- `hello-opencode/04-tool-system.md` — 工具调用机制：Tool 注册、权限控制、执行闭环、结果写回 Durable State
- `hello-opencode/05-state-management.md` — 状态管理：Durable State、消息持久化、并发占位与历史回放
- `hello-opencode/06-extension-mcp.md` — 扩展性：MCP 集成链路、Plugin 加载、新增工具的修改点
- `hello-opencode/07-error-security.md` — 错误处理与安全性：异常捕获、重试策略、认证鉴权、敏感信息隔离
- `hello-opencode/08-performance.md` — 性能与代码质量：流式传输、SSE、Bun runtime 优势、优缺点分析

【格式铁律】
- 每个主题必须独立成一个 `.md` 文件，严禁将多个主题合并写入同一个文件
- 代码引用须标注完整文件路径和行号，关键流程须附 Mermaid 顺序图（neutral 主题，尽量左右布局）

该项目为 Bun + TypeScript 全栈架构，重点分析：
- Bun runtime 提供的能力（Bun.serve、Bun.spawn）与 Node.js 的差异点
- Session 持久化与 Durable State 机制（`session/processor.ts`、`message-v2.ts`）
- MCP 集成链路与 Provider 抽象层

【现有文档处理】开始前必须：
1. 读取 `hello-opencode/` 目录的全部现有文件（当前有 `A00-mainline-index.md`、`A01`–`A07`、`B01`–`B15`、`C01`–`C02` 等大量已有文档）
2. 逐一读取已有文件内容，识别每篇对应哪个主题，避免重复撰写已覆盖的内容
3. 对目标文件清单中的每个主题：若已有 A/B/C 系列文档覆盖该主题，以现有内容为基础重构并迁移进新命名文件，而不是从零写；旧文件顶部加注整合说明后保留
4. 以 `A00-mainline-index.md`、`A04-session-loop.md` 等现有高质量文档的写作风格（包含调用链表格、代码跳点标注）为基准
