遵循 `hello.txt` 中的要求，对 `codex/` 目录下的工程进行深度分析，并将生成物写入 `hello-codex/` 目录。

【强制输出结构】必须按以下文件清单逐一创建独立文件，每个文件只写对应主题，不得合并：

- `hello-codex/00-codex_ko.md` — Kickoff 索引：列出所有子篇超链接 + 最短源码阅读路线（先读哪个文件、再读哪个）
- `hello-codex/01-architecture.md` — 架构全景：仓库形态、crate/package 拓扑、分层模型、核心抽象
- `hello-codex/02-startup-flow.md` — 启动链路：入口点、CLI 参数解析、初始化顺序、Subcommand 分发
- `hello-codex/03-agent-loop.md` — 核心执行循环：Agent 决策链、Prompt 构建、LLM 调用、流式响应处理
- `hello-codex/04-tool-system.md` — 工具调用机制：工具注册、权限控制、执行闭环、结果回传
- `hello-codex/05-state-management.md` — 状态管理：Thread/Turn/ThreadItem 模型、持久化、并发控制
- `hello-codex/06-extension-mcp.md` — 扩展性：MCP server 集成、Plugin/Skill 加载、新增工具的修改点
- `hello-codex/07-error-security.md` — 错误处理与安全性：异常捕获、重试策略、沙箱隔离、敏感文件防泄漏
- `hello-codex/08-performance.md` — 性能与代码质量：大文件处理、流式传输、优缺点分析、潜在改进点

【格式铁律】
- 每个主题必须独立成一个 `.md` 文件，严禁将多个主题合并写入同一个文件
- 代码引用须标注完整文件路径和行号，关键流程须附 Mermaid 顺序图（neutral 主题，尽量左右布局）

该项目为 Rust + TypeScript 混合架构，重点分析：
- `codex-rs/` 中 Rust 核心与 TypeScript CLI 之间的 FFI/IPC 边界（N-API 或 stdio 协议）
- Rust 侧的 Agent 执行循环（`core/src/thread_manager.rs`）与工具分发机制
- TypeScript 侧的 TUI 渲染与状态管理

【现有文档处理】开始前必须：
1. 读取 `hello-codex/` 目录的全部现有文件（当前有 `00-codex_ko.md` 至 `07-project-init-analysis.md` 等）
2. 逐一读取已有文件内容，识别哪些主题已被覆盖、哪些章节仍然缺失
3. 将 `07-project-init-analysis.md` 中与上述文件清单对应的内容分别提取合并进对应的目标文件
4. 以 `01-repo-shape.md`、`02-rust-runtime-and-cli.md` 等现有高质量文档的写作风格为基准
